{
  "name": "trae-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "virtual-lab"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm --prefix virtual-lab run dev",
    "build": "npm --prefix virtual-lab run build",
    "preview": "npm --prefix virtual-lab run preview"
  }
}
